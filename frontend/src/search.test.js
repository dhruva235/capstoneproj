describe('Search', () => {
    it('Should be true', () => {
       const test = true;
       expect(test).toBe(true);
    });
 });
 describe('Read', () => {
    it('Should be true', () => {
       const test = true;
       expect(test).toBe(true);
    });
 });
 describe('Profile', () => {
    it('Should be true', () => {
       const test = true;
       expect(test).toBe(true);
    });
 });
 describe('About', () => {
    it('Should be true', () => {
       const test = true;
       expect(test).toBe(true);
    });
 });
 describe('Create', () => {
    it('Should be true', () => {
       const test = true;
       expect(test).toBe(true);
    });
 });
 describe('Search', () => {
    it('Should be true', () => {
       const test = true;
       expect(test).toBe(true);
    });
 });
 describe('Songs', () => {
    it('Should be true', () => {
       const test = true;
       expect(test).toBe(true);
    });
 });
 describe('Example', () => {
    it('Should be true', () => {
       const test = true;
       expect(test).toBe(true);
    });
 });
 describe('Search', () => {
    it('Should be true', () => {
       const test = true;
       expect(test).toBe(true);
    });
 });
 describe('Search', () => {
    it('Should be true', () => {
       const test = true;
       expect(test).toBe(true);
    });
 });